---
- name: Install and start Apache
  hosts: webservers
  become: yes

  tasks:
    - name: Ensure Apache is installed
      yum:
        name: nginx
        state: present

    - name: Ensure Apache is running
      service:
        name: nginx
        state: started
